/**
 * TODO
 *
 * Follow algorithm
 */

var Follow = function (gramatica) { };

Follow.prototype = function (gramatica) {
  var map = Array.prototype.map;
};

if (require.main === module) {
  var follow = new Follow();
  console.log(follow);
}
